# Really Good Advices 🏠

**Веб-сайт для допомоги у веденні домашнього господарства**

---

## Опис проєкту

"Really Good Advices" — це освітня платформа, створена для людей, які прагнуть навчитися самостійно вести домашнє господарство. Сайт надає практичні поради з прибирання, приготування їжі, прання та догляду за рослинами.

### Місія проєкту
Допомогти користувачам впевнено вирішувати побутові питання через зрозумілі покрокові інструкції та перевірені життям поради.

### Цільова аудиторія
- Студенти, які починають самостійне життя
- Новоспечені батьки
- Люди, які хочуть покращити навички ведення домашнього господарства

---

## Структура проєкту

```
web_dev_iasa/
│
├── main_pages/                         # Головні сторінки сайту
│   ├── index.html                      # Головна сторінка
│   ├── about.html                      # Про нас
│   └── contacts.html                   # Контакти
│
├── advices/                            # Розділ з порадами
│   ├── all_advices.html                # Каталог всіх порад
│   └── cooking/                        # Категорія "Готування їжі"
│       ├── category-cooking.html       # Головна сторінка категорії
│       └── advices_cooking/            # Окремі поради з готування
│           └── advice-cooking-pelmens.html  # Як варити пельмені
│
├── images/                             # Зображення
│   ├── about/                          # Зображення для сторінки "Про нас"
│   └── cooking_advices/                # Зображення для порад з готування
│       └── pelmenes/                   # Зображення для поради про пельмені
│
├── css/                                # Стилі
│   ├── style.css                       # Головний CSS файл (імпортує інші)
│   ├── base.css                        # Базові стилі (змінні, типографіка)
│   ├── layout.css                      # Структура сторінки (header, nav, footer)
│   ├── components.css                  # Компоненти (кнопки, форми, картки)
│   ├── responsive.css                  # Медіа-запити для адаптивності
│   └── logo_advices.png                # Логотип сайту
│
├── js/                                 # JavaScript (не використовується)
│   └── new_file.js
│
└── README.md                           # Опис проєкту та його структури
```

---

## Сторінки сайту

### 1. **index.html** — Головна сторінка
- Вітання та опис платформи
- Логотип (клікабельний, веде на головну)
- Навігаційне меню (Головна | Поради | Про нас | Контакти)
- Burger-меню для мобільних пристроїв (CSS-only)
- **Картки категорій порад** (5 маленьких карток з іконками)
- **Форма підписки на новини** (ім'я + email + валідація)

### 2. **about.html** — Про нас
- Логотип (клікабельний)
- Навігаційне меню з burger-меню для мобільних
- Місія проєкту
- Візуальне порівняння "До/Після"
- Пояснення концепції

### 3. **contacts.html** — Контакти
- Логотип (клікабельний)
- Навігаційне меню з burger-меню для мобільних
- Email, Telegram, Instagram, телефон (стилізовані посилання)
- **Форма зв'язку** (вибір способу + контактна інформація + валідація)

### 4. **all_advices.html** — Усі поради
- Логотип (клікабельний)
- Навігаційне меню з burger-меню для мобільних
- **Великі картки категорій** (5 карток з іконками, категоріями, заголовками та описами)

### 5. **category-cooking.html** — Готування їжі
- Логотип (клікабельний)
- Навігаційне меню з burger-меню для мобільних
- Опис категорії
- **Картки порад з готування** (5 карток без іконок, тільки заголовки та описи)

### 6. **advice-cooking-pelmens.html** — Як зварити пельмені
- Логотип (клікабельний)
- Навігаційне меню з burger-меню для мобільних
- Передмова + 3 зображення
- Список інгредієнтів
- Покрокові інструкції
- Фото процесу 

---

## Технології

- **HTML5** — семантична розмітка
- **CSS3** — повна стилізація з використанням сучасних можливостей
- **JavaScript** — не використовується

### Використані HTML-елементи:
`<header>`, `<nav>`, `<main>`, `<section>`, `<footer>`
`<h1>`, `<h2>`, `<h3>`, `<h4>` — заголовки
`<p>`, `<ul>`, `<ol>`, `<li>`
`<a href="">` — посилання
`<img src="" alt="">` — зображення
`<form>`, `<input>`, `<select>`, `<label>`, `<button>`, `<textarea>`
`<div>` — контейнери для карток

### Використані CSS технології:

#### **1. CSS Variables (Custom Properties)**
Визначено у [base.css](css/base.css:4-17):
- `--primary-green: #62B880`
- `--white: #FFFFFF`
- `--light-green: #E1E9E1`
- `--dark-green: #29693F`

#### **2. Модульна структура CSS**
Файл [style.css](css/style.css) імпортує всі модулі через `@import`:
- `base.css` — змінні, reset, типографіка
- `layout.css` — структура сторінки (flexbox)
- `components.css` — компоненти (кнопки, форми, 3 типи карток)
- `responsive.css` — медіа-запити для всіх пристроїв

#### **3. Responsive Typography з clamp()**
Fluid typography у [base.css](css/base.css:27-67):
```css
html { font-size: clamp(14px, 1vw + 0.5rem, 18px); }
h1 { font-size: clamp(1.8rem, 5vw, 2.5rem); }
```

#### **4. CSS Grid для карток**
Адаптивні сітки з `auto-fit` у [components.css](css/components.css:195-344):
- Великі картки з категоріями порад: `repeat(auto-fit, minmax(280px, 1fr))`
- Маленькі картки з категоріями порад для головної сторінки: `repeat(auto-fit, minmax(150px, 1fr))`
- Картки для сторінки advices/cooking/category-cooking.html: `repeat(auto-fit, minmax(250px, 1fr))`

#### **5. Flexbox для layouts**
Header, navigation, footer у [layout.css](css/layout.css)

#### **6. CSS-Only Burger Menu**
Реалізація у [layout.css](css/layout.css:70-94) та [responsive.css](css/responsive.css:109-151):
- Прихований checkbox (`input[type="checkbox"]`)
- Label як кнопка-іконка
- Селектор `~` для показу меню: `.burger-toggle:checked ~ .nav-menu`

#### **7. HTML5 Form Validation**
Стилізація у [components.css](css/components.css:136-156):
- `:required:valid` — зелена рамка
- `:required:invalid` — стандартна рамка
- `.error-message` — червоні повідомлення про помилки

#### **8. Responsive Design**
Breakpoints у [responsive.css](css/responsive.css):
- Mobile: `< 768px`
- Mobile small: `< 480px`
- Mobile extra small: `< 360px`
- Tablet: `768px - 1024px`
- Desktop: `> 1024px`
- Large desktop: `> 1400px`

#### **9. Transitions and Hover Effects**
Анімації для карток, кнопок, посилань:
```css
transition: all 0.3s ease;
transform: translateY(-8px);
box-shadow: 0 12px 24px rgba(98, 184, 128, 0.2);
```

---


## Статистика

- **HTML сторінок:** 6
- **CSS файлів:** 5 (модульна структура)
- **Форм:** 2 (з HTML5 валідацією)
- **Типів карток:** 3 (великі, малі, текстові)
- **Зображень:** 6+
- **Категорій порад:** 5
- **Повних порад:** 1
- **Адаптивних breakpoints:** 6

---

## Результат перевірки з W3C validator, виявлені помилки
- зайве додавання `/` у непарних тегів
Приклад:
було: `<meta charset="UTF-8" />`
треба: `<meta charset="UTF-8">`

- error при виборі опції зв'язку в contacts.html через встановлене значення "Email" як дефолтне
треба: поставити дефолтним значення "Оберіть", яке при натисканні на кнопку вибору неможливо обрати, тож юзер має для себе обрати потрібний варіант

- `<section>` без заголовків
треба: додати заголовки за потреби, якщо ні - змінити на `<div>`

- image всередині списку
треба: винести за межі списку

==> всі помилки було виправлено, наразі валідація не виявляє жодних помилок

---

## Дизайн та стилізація

### Brand Book
Проєкт дотримується фірмового стилю:

**Кольори:**
- Основний зелений: `#62B880`
- Білий: `#FFFFFF`
- Світло-зелений: `#E1E9E1`
- Темно-зелений: `#29693F`

**Типографіка:**
- Шрифт: Roboto (Google Fonts)
- Ваги: 400 (Regular), 500 (Medium), 700 (Bold)
- Адаптивний розмір через `clamp()`

**Візуальний стиль:**
- Мінімалістичний дизайн
- Округлі кути (4px для кнопок, 8-12px для карток)
- М'які тіні з зеленими відтінками
- Плавні переходи (0.3s ease)

### Компоненти

#### **1. Логотип та Header**
- Логотип зліва, заголовок справа
- Від'ємні margins (-40px desktop, -45px mobile) для щільного розташування
- Вертикальне вирівнювання через `translateY()`
- Клікабельний логотип веде на головну сторінку

#### **2. Навігація**
- Desktop: горизонтальне меню по центру
- Mobile: burger-меню (CSS-only, без JavaScript)
- Checkbox + label техніка для розгортання
- Абсолютне позиціонування меню з `z-index: 1000`

#### **3. Картки (3 типи)**

**Великі картки** ([all_advices.html](advices/all_advices.html)):
- Іконка в круглому контейнері (90px)
- Категорія (uppercase, зелений колір)
- Заголовок (1.5rem, bold)
- Опис (1rem, сірий)
- Hover: піднімається на 8px, зелена тінь

**Малі домашні картки** ([index.html](main_pages/index.html)):
- Компактні (140px мінімальна висота)
- Тільки іконка (65px) + заголовок
- Grid: 2 колонки на мобільних
- Hover: піднімається на 5px

**Текстові картки** ([category-cooking.html](advices/cooking/category-cooking.html)):
- Без іконок
- Заголовок + опис (вирівняно зліва)
- Мінімалістичний вигляд
- Hover: піднімається на 6px

#### **4. Форми**
- Світло-зелений фон
- Зелена рамка при фокусі
- HTML5 валідація з візуальним feedback
- Valid поля: зелена рамка
- Invalid поля: червоні повідомлення про помилки

#### **5. Кнопки**
- Primary: зелений фон, білий текст
- Secondary: світло-зелений фон, темний текст
- Hover: темніший відтінок + підняття на 2px
- Active: повернення в початкову позицію

### Responsive Design

**Mobile (< 768px):**
- Burger-меню замість горизонтальної навігації
- Логотип 220px, щільне розташування з заголовком
- Картки в 1-2 колонки
- Повноширинні кнопки

**Tablet (768px - 1024px):**
- Зменшені відступи
- Адаптивна навігація

**Desktop (> 1024px):**
- Максимальна ширина контенту: 1200px
- Логотип 350px
- Картки в 2-4 колонки (auto-fit)

### Safari Support
Для локального тестування в Safari потрібно:
1. Увімкнути **Develop menu**: Preferences → Advanced → Show Develop menu
2. Увімкнути **Disable local file restrictions**: Develop → Disable Local File Restrictions

Альтернатива: використання локального сервера (див. [MOBILE-TESTING.md](MOBILE-TESTING.md))

---

## Тестування та оптимізація

### Browser Testing
Сайт протестовано на:
- Google Chrome (Desktop & Mobile)
- Safari (Desktop & Mobile) з налаштуваннями для локальних файлів
- Різні розміри екранів (360px - 1400px+)


### Image Optimization
Зображення оптимізовані:
- Конвертація `.jpg` → `.webp` для зменшення розміру
- Оптимізація через ImageOptimizer

### Performance Features
- Fluid typography через `clamp()` замість fixed sizes
- CSS Grid з `auto-fit` для автоматичної адаптації
- Transitions тільки на необхідних властивостях
- Мінімалістичний підхід без зайвих елементів

### Відомі особливості Safari
- Вимагає налаштування "Disable Local File Restrictions" для роботи @import
- Рекомендується використовувати локальний сервер замість `file://` протоколу

---

## Контакти

- **Email:** reallygood@advices.com
- **Telegram:** @reallygoodadvices
- **Instagram:** @really.good.advices
- **Телефон:** +38 099 111 22 33

---


### Структура CSS
- `css/style.css` — головний файл, імпортує всі інші
- `css/base.css` — змінні, reset, типографіка
- `css/layout.css` — структура (header, nav, footer)
- `css/components.css` — компоненти (кнопки, форми, картки)
- `css/responsive.css` — медіа-запити

### Класи карток
```html
<!-- Великі картки (all_advices.html) -->
<div class="advice-cards-grid">
  <a href="#" class="advice-card">...</a>
</div>

<!-- Малі картки (index.html) -->
<div class="home-cards-grid">
  <a href="#" class="home-card">...</a>
</div>

<!-- Текстові картки (category-cooking.html) -->
<div class="cooking-cards-grid">
  <a href="#" class="cooking-card">...</a>
</div>
```

### CSS Variables
```css
var(--primary-green)   /* #62B880 */
var(--white)           /* #FFFFFF */
var(--light-green)     /* #E1E9E1 */
var(--dark-green)      /* #29693F */
```

---

## Розподіл роботи між авторами проєкту

**Полюлях Ольга** - створення структури проєкту та файлів (HTML код), створення контенту для наповнення, CSS стилізація

**Лавріненко Олександра** - авторство ідеї проєкту, створення контенту для наповнення, внесення правок в структуру проєкту та наповнення (HTML код) файлів, CSS стилізація

---

**© 2025 Really Good Advices**

